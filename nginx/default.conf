server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Serve static assets for afjk.jp and subpaths (including /pipe)
  location / {
    try_files $uri $uri/ /index.html;
  }

}
